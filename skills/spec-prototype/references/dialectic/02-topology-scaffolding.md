# Dialectic Slice: Topology & Scaffolding (Frontier 2)

> Dynamic projection module for Spatial Density, Layout Scaffolding & Attention Centroid.
> Load strictly during Round 2 of Stage 1 co-creation.

## 1. Prerequisites Check
- Verify Round 1 (Metaphor & Character) is settled.
- Derive spatial distribution directly from the settled physical metaphor.

## 2. ASCII Wireframe Projections (NO COLOR, ONLY STRUCTURE)
Present 2~3 distinct spatial layouts via pure text ASCII wireframes:

### Option A: Synchronized Multi-Column Workbench (对标 Linear / CAD)
```text
┌─ Source / Object List ─┐┌─ Central Resolution Matrix ─┐┌─ Authority Gate / Inspector ─┐
│ • Pinned entities (320px)││ • Side-by-side diff & form ││ • Invariants readiness checklist│
│ • State status chips    ││ • Decisive inputs (flex)    ││ • Sealed commit trigger (280px)│
└─────────────────────────┘└──────────────────────────────┘└────────────────────────────────┘
```
- *Trade-off*: Maximum information throughput, zero modal disruption; requires >=1280px viewport.

### Option B: Focused Progressive Flow with Drawer (对标 Stripe / Notion)
```text
┌─ Global Entity Header & Primary Metrics ──────────────────────────────────────────────────┐
│ Primary workspace table / list (840px measure max)                                       │
│ [Row Action] ──► Discloses contextual slide-over drawer for parameter adjustment          │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```
- *Trade-off*: Low initial cognitive burden, fluid responsive reflow; multi-step comparison requires drawer toggling.

### Option C: Infinite Canvas & Contextual Inspector (对标 Figma / Linear Workflow)
```text
┌─ Palette / Minimap (240px) ─┐┌─ Infinite Graph / Plane (flex) ──────────────────────────┐┌─ Contextual Inspector (320px) ─┐
│ • Object stencil library    ││ [Node: Draft] ──(edge)──► [Node: Authority Seal]         ││ • Selected entity properties   │
│ • Viewport navigation HUD   ││               ▲                                          ││ • Invariant checklist triggers │
│ • Zoom & pan coordinates    ││               └─ Focus hover box (spatial coordinates)   ││ • High-hazard commit actions   │
└─────────────────────────────┘└──────────────────────────────────────────────────────────┘└────────────────────────────────┘
```
- *Trade-off*: Maximum spatial freedom and macroscopic comprehension; high cognitive load for linear tasks, requires responsive foldback to card stream on viewports <1024px.

## 3. Interaction Resistance & OOUX Card Ladder
- Map core action triggers to Container Proximity Ladder (Level 0~4).
- High-hazard actions MUST live in Proximity Level >= 2 (dedicated inspector or modal).
- Routine actions live in Level 0/1 (direct inline chip or table row action).

## 4. Ratchet Settlement
Lock visual centroid, layout profile (`adaptive-workspace` or `editorial-dossier`), and Density/Rhythm values.
