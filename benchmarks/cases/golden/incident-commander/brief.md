我们运维团队需要一个值班用的集群事故处置台。

值班的 SRE 要在上面看几个区域集群的异常、判断影响面、定位到具体的机器，然后把机器下线处理，最后把这次事故的处置过程收尾。

现在的痛点是一出事故信息就爆炸，大家在不同面板之间来回切，看错数字和误操作都发生过——上个月有人直接把生产节点一键排空了，事后没法回滚。

用的人基本都在桌面大屏上干活，偶尔在手机上瞄一眼事故状态。速度要求是越快越好，但绝对不能操作错。

第一版只要覆盖一件事：大规模雪崩时，主备指挥官怎么把事故安全处置完。
