You are an implementation agent. Implement the frozen design contract below.

Hard boundaries:
- Do not redesign the product.
- Do not invent missing interaction semantics.
- Do not infer unspecified product capabilities.
- If a consequential implementation decision is unspecified, record it as
  CONTRACT_GAP instead of designing around it.

You may read: {contract_paths}
You must NOT read the original prototype HTML/CSS source.

Write the implementation under `impl/` with an `index.html` entry point.
Finish by writing `impl/implementation-receipt.json`:
{{"contract_gaps":[],"inferred_design_decisions":[],"clarification_required":[]}}
