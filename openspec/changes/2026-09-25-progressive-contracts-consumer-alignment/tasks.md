# Tasks

## 1. Wave 3 — Progressive contracts and uni-directional tokens

- [ ] T-01 Progressive schema tiers, meso slots, and invariant retirement in canonical IR
  - Depends on: none
  - Anchors: skills/spec-prototype/scripts/compile_spec_ir.py:compile_spec_ir, skills/spec-prototype/schemas/prototype-spec.v1.json, tests/test_contract_seam_fidelity.py
  - Write scope: skills/spec-prototype/scripts/compile_spec_ir.py, skills/spec-prototype/schemas/prototype-spec.v1.json, tests/test_contract_seam_fidelity.py
  - Verify with: python3 -m pytest -q tests/test_contract_seam_fidelity.py
  - Verify tier: unit
  - Action: Introduce progressive schema tiers in prototype-spec.v1.json and compile_spec_ir.py: `intent_spec` requires only problem thesis, topology, and five-axis/craft intent (Stage 1 admission, no state_model requirement); `execution_spec` requires the full state machine and action contracts (Stage 3/4 admission). Compile IR emits the tier it can legitimately derive and stamps which tier it is. Structurally support meso assembly slots: `layout_directives.massing_pattern` (information-topology constructs), `interaction_spec.kinematics` (spatio-temporal continuity protocol), `visual_directives.data_syntax` (data micro-construct syntax); absence of a declared massing smooths to a fallback without blocking compilation. Remove the hardcoded injected telemetry/4096 GPU template invariants from compile_spec_ir.py (~L548-552 and the invariants array construction): invariants without an authored source are not injected; only discussion/Spec-authored invariants reach the IR. Fix the state-id word-splitting defect so full identifiers like `interaction/inspecting` are passed through intact to state_model entries. Retarget in-scope tests that lock retired behavior: tests/test_canonical_spec_ir.py (`assert ir["invariants"]` non-empty at ~:88 must become "invariants empty unless authored", plus new Stage-1 intent_spec passes validation and state ids stay whole); tests/test_contract_seam_fidelity.py seam assertions updated for tiered emission. Do not touch lint_spec_contracts.py (owned by T-02).
  - Specimen: Positive — a Stage 1 spec with thesis, topology, and craft intent but no states compiles and validates as intent_spec. Boundary — a Stage-1-only IR submitted where execution_spec is required fails with the missing-state message; an IR with authored invariants keeps them and an unauthored one injects none; `interaction/inspecting` survives compilation as one identifier.

- [ ] T-02 Tokens uni-directional derivation and strict freshness fuse
  - Depends on: T-01
  - Anchors: skills/spec-prototype/scripts/compile_tokens.py, skills/spec-prototype/scripts/lint_spec_contracts.py:277-298, tests/test_tokens.py, tests/test_craft_stack.py
  - Write scope: skills/spec-prototype/scripts/compile_tokens.py, skills/spec-prototype/scripts/lint_spec_contracts.py, tests/test_tokens.py, tests/test_craft_stack.py
  - Verify with: python3 -m pytest -q tests/test_tokens.py tests/test_craft_stack.py
  - Verify tier: unit
  - Action: Establish the one-way generation flow Discussion/Spec IR → compile_tokens → tokens.css. Retire the formal-mode reverse reconciliation entry in compile_tokens.py: hand edits to tokens.css that disagree with the source digest are detected, the tokens state is marked `out_of_sync`, and downstream consumption is hard-rejected with the drift named. Align lint_spec_contracts.py (~L277-298) with the T-01 tiered schema so an intent_spec validates at its tier and does not demand execution_spec fields, and so out_of_sync tokens fail the lint. Tier-admission presence rule in lint_canonical_spec_ir: an execution_spec claim fails E021 only when the state_model/actions key is absent or None, or when every state_model section is empty; a present-but-empty actions array with a populated state_model lints clean (this keeps the T-01 valid_ir fixture green on the integrated tree). Complete the compiler's consumption of canonical craft_stack and palette so tokens parameters derive from authored dials/palette rather than probe heuristics. Retarget in-scope tests that lock retired behavior: tests/test_tokens.py formal-mode tests (~:160-230) must assert the new fuse (edit-after-compile → out_of_sync → downstream reject; palette-only edits cannot pass the dial-annotation freshness check); tests/test_craft_stack.py fixture (`interaction/inspecting` at ~:37) must compile through the T-01 whole-identifier behavior.
  - Specimen: Positive — compile from discussion, tokens.css matches source digest, downstream passes. Boundary — flip one palette color in tokens.css after compile: freshness check must fail (not just the dial annotations) and downstream admission is rejected naming tokens.css as out_of_sync.

## 2. Wave 4 — Consumer freedom and tiered evaluation

- [ ] T-03 Builder/Critic meso consumption and dual-dimension review
  - Depends on: T-01
  - Anchors: agents/spec-prototype-builder.md, agents/spec-prototype-critic.md, skills/spec-prototype/references/02-craft-methods/visual-craft.md, skills/spec-prototype/references/02-craft-methods/data-information.md, skills/spec-prototype/references/02-craft-methods/ia-interaction.md, tests/test_pipeline.py:145-175
  - Write scope: agents/spec-prototype-builder.md, agents/spec-prototype-critic.md, skills/spec-prototype/references/02-craft-methods/visual-craft.md, skills/spec-prototype/references/02-craft-methods/data-information.md, skills/spec-prototype/references/02-craft-methods/ia-interaction.md, tests/test_pipeline.py
  - Verify with: python3 -m pytest -q tests/test_pipeline.py
  - Verify tier: unit
  - Action: Builder prompt: consume meso constructs — build spatial center of gravity from `massing_pattern`, implement deformation and focus-restore transitions from `kinematics`, implement compact alignment and micro-trend charts from `data_syntax`; remove the uniform large-radius, wide-padding, negative-tracking biases so minimal, native, and high-density styles are all reachable from real business constraints. Critic prompt: dual-dimension review — engineering contract (state-machine coverage, DOM reachability, accessibility) AND cognitive quality (visual signal-to-noise ratio, state-transition spatio-temporal momentum conservation, negative-space breathing); never pass a mediocre genericized UI merely for being compliant and error-free; block prototypes that lose context via crude `display: none`; do not demand frosted-glass or decorative layers on dense consoles. Update the three craft-method references so their defaults are declared-attribute-driven rather than fixed style tables. Retarget in-scope tests that lock retired text: tests/test_pipeline.py:152-154 asserts "Optical Concentric Radii"/"Tabular numerals" in visual-craft.md — update to the new ownership wording; keep quality-floor.md and discussion-template assertions (out of scope) untouched.
  - Specimen: Positive — a dense console Spec with `massing_pattern` declared produces Builder guidance naming spatial hierarchy from that pattern and no decorative-layer demand. Boundary — a Critic review of a context-destroying `display: none` prototype is blocked even when all engineering checks pass; a compliant but genericized flat UI fails the cognitive dimension.

- [ ] T-04 Tiered evidence chain in quality verification and eval alignment
  - Depends on: none
  - Anchors: skills/spec-prototype/scripts/verify_prototype_quality.py, skills/spec-prototype/evals/eval.yaml, tests/test_dialectic_enhancements.py:56-173, tests/test_v10_integrity.py:37-58
  - Write scope: skills/spec-prototype/scripts/verify_prototype_quality.py, skills/spec-prototype/evals/eval.yaml, tests/test_quality_tiers.py, tests/test_dialectic_enhancements.py, tests/test_v10_integrity.py
  - Verify with: python3 -m pytest -q tests/test_quality_tiers.py tests/test_dialectic_enhancements.py
  - Verify tier: unit
  - Action: Build the L1/L2/L3 tiered degradation chain in verify_prototype_quality.py: L1 = DOM/ARIA/data-state structural checks (always available); L2 = computed-style checks (available when a style engine is reachable); L3 = screenshot comparison (best-effort). When the headless browser environment, fonts, or GPU are missing, degrade gracefully to L1/L2 and report the result as environment-not-ready with the tier reached — never as a code-assertion failure. The evidence record names the tier and the reason for each skipped tier. Fix eval.yaml include path (~L7): point `agents/**` at the repo-root `agents/` originals (spec-prototype-builder.md, spec-prototype-critic.md) instead of the skill-local directory that does not contain them; add `evals/cases/confirmed-resume.yaml` to the case list and establish the same-content blind-comparison baseline case config. Retarget in-scope tests that lock current behavior: tests/test_dialectic_enhancements.py assert_quality callers (~:56-173) must pass under L1/L2 degradation (their fixtures have no browser); tests/test_v10_integrity.py empty-prototype blocking (~:37-58) stays an L1 hard failure — update if the tier labeling changes the failure shape. Create tests/test_quality_tiers.py owning the new tier-degradation behavior (browser-missing → L1/L2 pass with environment label; screenshot tier skipped with reason; L1 failures still block).
  - Specimen: Positive — a prototype with correct DOM/ARIA/data-state but no browser available reports success at L1/L2 with "environment_not_ready" for L3. Boundary — a prototype failing an L1 structural check is blocked even when L2/L3 are unavailable; a tiered evidence record always names which tiers ran and why the rest did not.
